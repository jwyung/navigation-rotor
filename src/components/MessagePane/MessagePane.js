import React, { PropTypes } from 'react';

const MessagePane = props => {
  return (
    <section className="message-pane" role="main">
    </section>
  );
}

MessagePane.propTypes = {
};

export default MessagePane;
