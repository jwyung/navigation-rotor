import React, { PropTypes } from 'react';
import './sidebar.css';

const Sidebar = props => {
  return (
    <section className="sidebar" role="complementary">
    </section>
  );
}

Sidebar.propTypes = {
};

export default Sidebar;
