import React, { PropTypes } from 'react';

const Sidebar = props => {
  return (
    <section className="sidebar" role="complementary">
    </section>
  );
}

Sidebar.propTypes = {
};

export default Sidebar;
